export const BREAKPOINTS = {
    sm: 640,   // 1 column → 2 columns
    md: 768,   // 2 columns → 3 columns  
    lg: 1024,  // 3 columns → 4 columns
    xl: 1280,  // Larger spacing, bigger text
} as const;